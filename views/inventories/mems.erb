<!DOCTYPE html>
<html>
  <head>
    <meta charset='UTF-8' />
    <title>Lab Systems Inventories - All Mems</title>
    <script type='text/javascript'>
    </script>

  </head>
  <body>
    <% if @errors %>
    <div id="errors">
        <%= @errors %><br>
    </div>
    <% end %>
    <br/>
    <h2>Memories</h2>
    <table>
    <tr>
        <th>ID</th>
        <th>Description</th>
        <th>Timestamp</th>
    </tr>
    <% @mems.each do |c| %>
    <% i = Inventory.find(c.inventory_id) %>
    <tr>
    <td><a href="./<%=c.inventory_id%>"><%=i.bmc_mac%></a></td>
    <td><%= c.description %>,<%= c.product %>,<%= c.vendor %>,<%= c.physid %>,<%=c.serial %>,<%=c.slot %>,<%=c.size %></td><td><%=c.timestamp%></td>
    <tr>
    <% end %>
    </table>


    <a href="/inventories">Back to Index</a>

</html>
